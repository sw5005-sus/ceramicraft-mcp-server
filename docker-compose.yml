services:
  mcp-server:
    build: .
    container_name: mcp-server-container
    env_file:
      - .env
    ports:
      - "${MCP_SERVER_PORT:-8080}:${MCP_SERVER_PORT:-8080}"
    restart: unless-stopped

networks:
  default:
    driver: "bridge"
